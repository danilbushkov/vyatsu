mod transport_task;


use crate::transport_task::input::input;
use crate::transport_task::parse::parse;
use crate::transport_task::potential_method::get_cycle;
use std::collections::{HashMap, HashSet, LinkedList};



fn main() {

    // let cycle = get_cycle(&HashMap::from(
    //     [
    //         (0, HashSet::from([2, 3])),
    //         (1, HashSet::from([2])),
    //         (2, HashSet::from([1, 3])),
    //         (3, HashSet::from([3])),
    //     ]
    // ), (1, 1));
    //let d = parse(input());
    //println!("{}", d);

    
}
